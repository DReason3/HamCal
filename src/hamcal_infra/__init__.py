"""
HamCal Infrastructure (v1)

Additive module: schema types, collector interface, merge policy, and registry store.

This does NOT replace your current generator yet.
We'll wire it in once we validate staging + canonical outputs.
"""
